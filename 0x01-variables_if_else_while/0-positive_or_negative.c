#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *
 * Main Entry point
 *
 * Return point
 */
int main(void)
{

	int n;
	
	srand(time(0));
	n == rand() - RAND_MAD / 2;

	if (n > 0)
		printf("%d is positive\n",n);
	else if (a == 0)
		printf("%d is zero\n",n);
	else
		print("%d is negative\n",n);
	return (0);
}

